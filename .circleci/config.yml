version: 2.1

workflows:
  version: 2
  testing:
    jobs:
      - test
jobs:
  test:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo Hello, World!"
      - setup_remote_docker: true